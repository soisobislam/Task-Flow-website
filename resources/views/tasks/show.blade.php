@extends('layouts.app')

@section('content')
<div class="mb-8"><p class="mb-2 text-sm font-medium uppercase tracking-[0.2em] text-cyan-300">{{ $task->project->name }}</p><h1 class="text-4xl font-semibold tracking-normal">{{ $task->title }}</h1><p class="mt-4 max-w-2xl whitespace-pre-line text-slate-400">{{ $task->description ?: 'No description added.' }}</p></div><div class="grid gap-5 md:grid-cols-4"><div class="rounded-2xl border border-white/10 bg-white/5 p-5"><p class="text-sm text-slate-400">Status</p><p class="mt-2 font-semibold text-cyan-200">{{ ucfirst(str_replace('_', ' ', $task->status)) }}</p></div><div class="rounded-2xl border border-white/10 bg-white/5 p-5"><p class="text-sm text-slate-400">Priority</p><p class="mt-2 font-semibold text-amber-200">{{ ucfirst($task->priority) }}</p></div><div class="rounded-2xl border border-white/10 bg-white/5 p-5"><p class="text-sm text-slate-400">Assigned to</p><p class="mt-2 font-semibold">{{ $task->assignee?->name ?: 'Unassigned' }}</p></div><div class="rounded-2xl border border-white/10 bg-white/5 p-5"><p class="text-sm text-slate-400">Created by</p><p class="mt-2 font-semibold">{{ $task->creator->name }}</p></div></div>@can('update', $task)<div class="mt-6 flex flex-wrap gap-2">@foreach (['todo', 'in_progress', 'review', 'completed'] as $status)<form method="POST" action="{{ route('tasks.status', [$task, $status]) }}">@csrf @method('PATCH')<button class="rounded-lg border border-white/15 px-3 py-2 text-sm text-slate-300 hover:border-cyan-300">{{ ucfirst(str_replace('_', ' ', $status)) }}</button></form>@endforeach</div>@endcan
@endsection
