@extends('layouts.app')

@section('content')
<div class="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-start"><div><p class="mb-2 text-sm font-medium uppercase tracking-[0.2em] text-cyan-300">Project</p><h1 class="text-4xl font-semibold tracking-normal">{{ $project->name }}</h1><p class="mt-3 max-w-2xl text-slate-400">{{ $project->description ?: 'No description added yet.' }}</p></div><div class="flex items-center gap-3"><span class="rounded-full bg-cyan-400/10 px-3 py-1 text-sm text-cyan-300">{{ str_replace('_', ' ', ucfirst($project->status)) }}</span>@can('create', [App\Models\Task::class, new App\Models\Task(['project_id' => $project->id])])<a href="{{ route('tasks.create', $project) }}" class="rounded-lg bg-cyan-300 px-4 py-2 text-sm font-semibold text-slate-950 transition hover:bg-cyan-200">Create task</a>@endcan</div></div>
<div class="grid gap-5 md:grid-cols-3"><div class="rounded-2xl border border-white/10 bg-white/5 p-5"><p class="text-sm text-slate-400">Manager</p><p class="mt-2 font-semibold">{{ $project->manager->name }}</p></div><div class="rounded-2xl border border-white/10 bg-white/5 p-5"><p class="text-sm text-slate-400">Timeline</p><p class="mt-2 font-semibold">{{ $project->start_date->format('M j') }} to {{ $project->deadline->format('M j, Y') }}</p></div><div class="rounded-2xl border border-white/10 bg-white/5 p-5"><p class="text-sm text-slate-400">Tasks</p><p class="mt-2 font-semibold">{{ $project->tasks->count() }}</p></div></div>
<div class="mt-8 rounded-2xl border border-white/10 bg-white/5 p-6"><h2 class="text-xl font-semibold">Team members</h2><div class="mt-4 flex flex-wrap gap-2">@forelse ($project->members as $member)<span class="rounded-full border border-white/10 px-3 py-1 text-sm text-slate-300">{{ $member->name }}</span>@empty<span class="text-sm text-slate-400">No members assigned yet.</span>@endforelse</div></div>
<div class="mt-5 rounded-2xl border border-white/10 bg-white/5 p-6"><h2 class="text-xl font-semibold">Tasks</h2><div class="mt-4 space-y-3">@forelse ($project->tasks as $task)<div class="flex flex-col justify-between gap-2 border-b border-white/10 pb-3 last:border-0 sm:flex-row"><span class="font-medium">{{ $task->title }}</span><span class="text-sm text-slate-400">{{ $task->assignee?->name ?: 'Unassigned' }} · {{ ucfirst(str_replace('_', ' ', $task->status)) }}</span></div>@empty<p class="text-sm text-slate-400">No tasks have been created for this project.</p>@endforelse</div></div>
@endsection
